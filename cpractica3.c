#include <stdio.h>
int main()
{
int x = 40;
if (x == 20)
   {
     printf("x vale 20");
   }
   else if(x==30)
       {
        printf("x vale 30");
       }
  else if(x==40)
       {
       printf("x vale 40");   
       }
      else
       {
       printf("No se reconce el valor de x");
       }
  printf("Esto se ejecutara siempre");
 return 0;
}
